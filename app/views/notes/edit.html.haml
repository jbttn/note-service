.row
  .liquid-form.paper-container
    = form_for(@note, :as => :note, :url => @note) do |f|
      = render 'notes/fields', f: f
      = f.submit 'Update', class: 'btn btn-primary btn-large', style: 'width: 100%;'
    %h1 
      %i Preview
    #note-preview