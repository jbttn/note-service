#note-header
  %h1= @note.title
  .pull-right
    = link_to "View Undistracted", undistracted_path(@note), class: 'btn btn-primary'
    = link_to "View Raw", '#raw-note', class: 'btn btn-primary'
#note-content.paper-container
  = markdown(@note.content)
%h1#raw-note Raw
%pre= @note.content