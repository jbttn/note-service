%table.table.table-striped.paper-container
  %thead
    %tr
      %th Title
      %th Owner
      %th Created
      %th Hits
  %tbody
    - @notes.recent.each do |note|
      %tr
        %td= link_to note.title, note
        %td= link_to note.owner.username, note.owner
        %td= "#{time_ago_in_words(note.created_at)} ago"
        %td= note.hits